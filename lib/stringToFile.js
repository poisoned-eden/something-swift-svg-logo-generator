const { writeFile } = require('fs/promises');

async function stringToFile(params)  {
    try {
        // TODO create path and filename
        // TODO write string to file specified
        return console.log()
    } catch (err) {
        console.err(err);
    }
}
module.exports = stringToFile;